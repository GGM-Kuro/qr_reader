import 'package:flutter/material.dart';

class MapsHistoyrPage extends StatelessWidget {
  const MapsHistoyrPage({super.key});

  @override
  Widget build(BuildContext context) {
        return Center(
            child: Text('History'),
        );
  }
}
